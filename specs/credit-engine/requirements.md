# Credit Engine Requirements

## Status

To be extracted from the recruiter-provided challenge PDF before business implementation starts.

## Rule

This file describes **what** the challenge requires. It must not contain implementation choices such as Express, Zod, Docker, or AWS unless the challenge itself requires them.

Each requirement should receive a stable identifier such as `REQ-001`.
